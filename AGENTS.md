# 仓库级协作约束

## 基本规则

- 所有文本文件统一使用 UTF-8 编码。
- 新增或修改函数时，按项目既有规范补充简洁清晰的中文注释。
- 仓库级稳定规则优先写在本文件；模块级、专题级和易变化的说明写在对应 Skill 与 `references` 文档中。

## 完成定义

- AI 完成范围：
  - 已按工程当前实际目录与入口完成代码、规则或文档接入。
  - 可自动执行的步骤已执行；若未执行，需明确说明阻塞原因与当前状态。
  - 若本次改动可能影响 Skill 文档事实，已完成一次 Skill 自检，并同步更新过期文档。
- 开发者完成范围：
  - 预制体拼装、Inspector 挂接、资源摆放、最终表现校验。
  - 需要在 Unity 编辑器内手动确认的资源状态、组件引用与运行结果。
- 交付判定：
  - 相关改动已落到正确目录。
  - 关键入口、规则与命名已接通。
  - 自动化步骤已执行或已说明未执行原因。
  - 人工收尾项已列明，且表述不含糊。
  - 若终端、`batchmode` 或脚本验证失败，需先区分环境问题与业务逻辑问题，不把执行环境失败直接误判为业务接入失败。

## Skill 文档同步与自检

- 新增或调整 Skill 后，需同步维护该 Skill 的自检配置，并注册到 `.agents/registries/skill-self-check.json`。
- 使用者明确要求自检，或 AI 判断改动可能导致 Skill 文档过期时，执行自检。
- 优先执行 `python .agents/scripts/skill_self_check.py`；若工作区存在无关脏改动，使用 `--base`、`--head` 或 `--path` 缩小范围。
- 自检目标是确认已注册 Skill 与仓库当前事实一致；若文档过期，仅补丁式更新受影响内容，不整篇重写。
- 若脚本执行失败，改为人工完成最小自检。
- 若本次任务触发了自检，回复中需说明：是否执行、命中的 Skill / 文档、已更新内容；未执行则说明原因。
